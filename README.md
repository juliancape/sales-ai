# Sales AI

Este proyecto utiliza Pydantic AI para interactuar con modelos de lenguaje.

## Configuración

1.  Copia el archivo `.env.example` a `.env`:
    ```bash
    cp .env.example .env
    ```
2.  Agrega tu `OPENAI_API_KEY` al archivo `.env`.

## Ejecución

```bash
uv run main.py
```